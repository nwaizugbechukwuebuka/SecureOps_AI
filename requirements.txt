# SecureOps_AI dependencies (runtime + test/dev)
# Pin versions for reproducible installs in CI. Update as needed.

# Runtime
fastapi==0.100.0
uvicorn[standard]==0.23.2
scikit-learn==1.5.0
numpy==1.26.4
PyYAML==6.0

# HTTP clients / testing
httpx==0.24.1

# Testing
pytest==7.4.0
pytest-asyncio==0.22.0

# Optional utilities
python-dotenv==1.0.0

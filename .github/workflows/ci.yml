Steps:

Checkout repository

Set up Python environment (matching your project version)

Install dependencies (requirements.txt)

Run linters (flake8, black --check, isort --check)

Run type checks (mypy)

Run tests (pytest, pytest-cov)

Upload coverage report to GitHub Actions
